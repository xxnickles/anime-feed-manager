import '@ionic/core';
import { akitaDevtools } from '@datorama/akita';
// import { setupConfig } from '@ionic/core';
akitaDevtools()
export default () => {
  // setupConfig({
  //   mode: 'ios'
  // });
};

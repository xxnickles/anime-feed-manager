@page "/Register"

<AppPageTitle Title="@PageTitle("Register")" />

<section class="mx-auto flex w-full max-w-sm flex-col items-center pt-8">
    <article class="card w-full bg-base-100 shadow-sm">
        <RegistrationForm Model="new RegisterViewModel()"/>
    </article>
</section>

@code {
}

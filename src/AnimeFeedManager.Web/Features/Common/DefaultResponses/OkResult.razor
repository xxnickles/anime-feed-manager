<div role="alert" class="alert alert-success" x-data="{ open: true }" x-show="open" x-transition.duration.500ms>
    <div>
        <span>@OkMessage</span>
    </div>
    <button class="btn btn-xs btn-circle btn-ghost" x-on:click="open = false">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
        </svg>`
    </button>
</div>

@code {
    [Parameter, EditorRequired] public string OkMessage { get; set; } = string.Empty;
}